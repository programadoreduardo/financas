import { Text, View } from 'react-native';
import { styles } from './styles';

export default function HistoricList() {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>HistoricList</Text>
        </View>
    );
}